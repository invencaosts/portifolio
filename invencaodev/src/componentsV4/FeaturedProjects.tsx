import Link from "next/link"; import { projects } from "@/content/projects";
export default function FeaturedProjects(){return <section id="projetos" className="py-24 px-6 md:px-12 lg:px-24 xl:px-40 scroll-mt-20"><div className="max-w-7xl mx-auto"><p className="section-label">03_PROJETOS_SELECIONADOS</p><h2 className="section-title">Código com contexto.</h2><p className="section-intro">Três projetos escolhidos pela profundidade técnica e pela relação com minha atuação — não pela data do último commit.</p><div className="grid lg:grid-cols-3 gap-5 mt-12">{projects.map((p,i)=><article key={p.slug} className="bento-card p-7 flex flex-col"><div className="flex justify-between font-mono text-[10px] text-primary-text uppercase"><span>PRJ_{String(i+1).padStart(2,"0")}</span><span>{p.status}</span></div><p className="font-mono text-xs uppercase text-foreground/50 mt-8">{p.category}</p><h3 className="font-headline text-2xl font-bold mt-2">{p.shortTitle}</h3><p className="text-foreground/65 leading-relaxed mt-4 flex-grow">{p.summary}</p><p className="text-sm mt-5"><span className="text-primary-text font-mono">PAPEL // </span>{p.role}</p><div className="flex flex-wrap gap-2 mt-5">{p.technologies.slice(0,5).map(t=><span className="tech-chip" key={t}>{t}</span>)}</div><div className="flex gap-4 mt-7 font-mono text-xs uppercase"><Link className="text-primary-text hover:underline" href={`/projetos/${p.slug}`}>Ver case →</Link>{p.links[0]&&<a href={p.links[0].href} target="_blank" rel="noopener noreferrer" className="hover:text-primary-text">Código ↗</a>}</div></article>)}</div></div></section>}
